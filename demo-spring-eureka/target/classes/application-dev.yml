variable:
  profiles: dev
server:
  port: 8083
spring:
  application:
    name: demo-spring-eureka
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://127.0.0.1:${server.port}/eureka